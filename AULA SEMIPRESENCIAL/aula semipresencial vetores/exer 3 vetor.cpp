#include<stdio.h>
main()
{
int 
vetor[30],x,y=0,valor;
 for(x=0;x<30;x++)
  scanf("%d",&vetor[x]);         
    }
 do
 {
  (vetor[y]>0)            	
  printf("Positivo %d \n",vetor[x]);
  y=y+1;
  }while (y<30);

}
