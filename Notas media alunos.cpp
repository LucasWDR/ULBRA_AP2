#include<stdio.h>
#include <conio.h>
main()
{
	int x;
	float nota1,nota2,somanota=0,mediaaluno,mediatotal,somatotal;
	for (x=1; x<=10; x++)
	printf ("\n digite a nota g1:")
    scanf("%f",&nota1);
	printf ("\n digite a nota g2 %i,x:")
   	scanf("%f", &nota2);
	somanota= nota1 + nota2;
	mediaaluno= somanota/2;
	if (mediaaluno >=6)
	printf ("media de nota por aluno é :%.2f,mediaaluno")
     mediatotal = somatotal/20
     printf ("\n A média da classe é :"%.2f,mediatotal);
	getche();
		
}
