#include <stdio.h> 
 main() 
 { 
int val,vetp[5],veti[5],i,j,p=0,i=0; 
      for (i=1; i<=30; i++)
 { 
   printf("Informe o %dº valor: ",i); 
   scanf("%d",&val); 

if (val % 2 == 0) 
{ 
vetp([p++] = val); 
if (p == 5){ 
printf("terminar leitura dos pares:"); 
for (j=0; j<5; j++) 
printf("\n %d", vetp[j]); 
p = 0;
}
} 
if (veti[i++] = val); 

if (i == 5) { 
printf("terminar leitura do impares:"); 
for (j=0; j<5; j++) 
printf("\n %d", veti[j]); 
i = 0; 
} 
} 
if{ 
printf("Vetor de pares final com %d números:", cp); 
for (j=0; j<p; j++) 
printf("\n %d", vetp[j]);
} 
if
{ 
printf("Vetor de ímpares final com %d números:", ci); 
for (j=0; j<i; j++) 
printf("\n %d", vetp[j]); 

}

}
